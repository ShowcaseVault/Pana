# Nothing real in this directory is ever committed. It holds SIP trunk
# credentials: anyone who reads them can place calls billed to our carrier
# account. Only the example template and this file are tracked.
*
!.gitignore
!*.example
