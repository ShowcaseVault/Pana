[general]
enabled = yes
; Bind to the container's loopback only. ARI is an unauthenticated-until-you-
; guess-the-password control channel that can originate calls, so it must
; never be reachable from outside. The media service reaches it by sharing
; the container's network namespace (see docker-compose).
bindaddr = 127.0.0.1
bindport = 8088
